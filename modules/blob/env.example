# Vercel Blob read/write token. Any existing Vercel Blob store on your team
# works. Uploads namespace under `agenticbuilder/<userId>/` so they coexist
# with other projects sharing the store. Get the token from Vercel Dashboard
# → Storage → Blob → your store → Read/Write token.
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
